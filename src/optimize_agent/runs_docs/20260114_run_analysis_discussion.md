1. ok interesting - is a subsequent fault

2. lets ignore that - only warning

3. the model often ignores its own computed variables in favor of "plausible" guesses for numbers, especially when the trajectory becomes long and the context is cluttered.
